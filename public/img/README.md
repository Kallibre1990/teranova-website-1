# /public/img — реальные фото (позже)

Сюда кладём **ТОЛЬКО реальные фотографии с личных визитов** на производство —
для конкретных проверенных карточек поставщиков (появятся позже).

- Это **НЕ фоновый слой.** Фоны и весь визуал рисуются **кодом** (CSS/SVG/canvas) —
  см. `docs/IMAGERY.md`.
- **Никакого стока, AI-картинок или чужих фото.**
- Пока реальных фото нет — папка пустая (кроме этого README); фон рисуется кодом
  (сгенерированная «ткань» `public/brand/tex-deep.webp` + свечение + контур + зерно).
